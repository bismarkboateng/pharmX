"use server"

type FormData = {
    name: string,
    email: string,
    password: string
}

export const createUser = async (formData: FormData) => {
    try {
        
    } catch (error) {
        
    }
}